<h1>Aca vas a mostrar todos los productos</h1>


<div class="contenedorProductos">
    {{#each productos}}
        <div>
            <p> {{this.title}} </p>
            <p> {{this.description}} </p>
            <p> {{this.price}} </p>
        </div>
    {{/each}}
</div>
